* more builtins
	* `pwd`
	* `which`/`type`
	* `alias`
	* `version`
	* `env`
	* `export`
	* `unset`
* piping (i/o)
  * first single stage pipelines
  * then multi stage
* output redirection
  * `> and >>` operators
  * `>` redirection
  * `>>` append
* input redirection
  * `<` operator
