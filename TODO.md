* piping (i/o)
  * first single stage pipelines
  * then multi stage
* output redirection
  * `> and >>` operators
  * `>` redirection
  * `>>` append
* input redirection
  * `<` operator
* background processes
  * non-blocking
  * `sleep 2 &`
* up/down arrow for history
